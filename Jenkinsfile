pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                //script to execute something on the jenkins server or jenkins agent
                // npm commands
              echo 'building the application'
            }
        }
      stage('test') {
            steps {
              echo 'testing the application'
                
            }
        }
      stage('deploy') {
            steps {
                echo 'deploying the application'
            }
        }
    }
}
